package main.java.exam.srp;class Cat {    private final int age;    private final String name;    Cat(int age, String name) {        this.age = age;        this.name = name;    }    void eat() {        System.out.println("고양이가 먹는다.");    }    void walk() {        System.out.println("고양이가 걷는다");    }    void speak() {        System.out.println("고양이가 야옹 운다");    }    String representation() {        return "고양이 나이는 : " + age + "고양이 이름은 : " + name;    }    // SRP는 단일 책임 원칙이다. 이 아래의 메서드(기능)들은 고양이의 책임이 맞을까?    // 고양이는 콘솔에 출력해주거나, 로그를 남기는 책임을 가지지 않는다.    // 이는 SRP를 위배하는 것이고    // 단일 책임을 가질 수 있게 Cat 클래스에서는 고양이의 상태를 나타내는 메서드만을 남겨놓고,    // Printer, Logger의 클래스를 만들어 Cat 클래스에서 상태를 조회해 각자의 책임에 맞게 구현하면 될 것 같다.    // Printer 클래스로 이전    //    void print() {    //        System.out.println("고양이 나이는 : " + age + "고양이 이름은 : " + name);    //    }    // Logger 클래스로 이전    //    void log() {    //        logger.info("고양이 나이는 : " + age + "고양이 이름은 : " + name);    //    }}
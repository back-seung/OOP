package main.java.point;public class GiftPoint {    private static final int MIN_POINT = 0;    final int value;    // 생성자를 public으로 만들면 의도하지 않은 용도로 사용될 수 있다.    // 예) 회원가입 포인트를 변경하고 싶을 때 코드 전체를 확인해야 한다.//    public GiftPoint(int value) {//        if (value >= MIN_POINT)//            throw new IllegalArgumentException("포인트를 0이상 입력해야 합니다");//        this.value = value;//    }    private GiftPoint(int value) {        this.value = value;    }    public static GiftPoint forStandardMemberShip() {        int point = GiftPointType.STANDARD_MEMEBERSHIP_POINT.getPoint();        return new GiftPoint(point);    }    public static GiftPoint forPremiumMemberShip() {        int point = GiftPointType.PREMIUM_MEMBERSHIP_POINT.getPoint();        return new GiftPoint(point);    }    boolean isEnough(ConsumptionPoint point) {        return this.value >= point.value;    }    GiftPoint consume(ConsumptionPoint point) {        if (!isEnough(point))            throw new IllegalArgumentException("포인트가 부족합니다.");        return new GiftPoint(this.value - point.value);    }}